# Base URL of the backend API
# Local dev: leave as /api (Vite proxy forwards to http://localhost:5000)
# Production: set to your deployed backend URL, e.g. https://sms-api.onrender.com/api
VITE_API_URL=/api
